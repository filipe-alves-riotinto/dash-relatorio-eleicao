# 📘 Projeto — Instruções de Execução

## 🚀 Pré-requisitos
Antes de iniciar o projeto, certifique-se de ter o **Python** instalado e de usar um ambiente virtual (recomendado).

---

## 📦 Instalar dependências
Execute o comando abaixo para instalar todas as bibliotecas necessárias:

```bash
pip install -r requirements.txt
```

## 🔐 Configurar variáveis de ambiente
Crie um arquivo .env na raiz do projeto contendo:

```bash
DATALAKE=https://datalake.psdb.org.br/baseDash
```

## ▶️ Executar a aplicação
Para iniciar o dashboard, rode:

```bash
streamlit run Estadual.py
```
